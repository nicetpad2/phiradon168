version: 1
formatters:
  default:
    format: '%(asctime)s | %(levelname)-8s | %(module)s:%(lineno)d | %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: default
root:
  level: INFO
  handlers: [console]
